<?php
/**
 * تنظیمات پنل آپلود
 * -------------------------------------------------------------
 * همه‌ی گزینه‌ها اینجا هست. اگر خواستی چیزی رو عوض کنی همین فایل کافیه.
 */
declare(strict_types=1);

return [

    /* ---------- عنوان و برندینگ ---------- */
    'site_title'    => 'پنل آپلود فایل',
    'site_subtitle' => 'فایل رو بکش و بنداز اینجا، لینکش رو بگیر',
    'footer_text'   => 'قدرت‌گرفته از پنل آپلود PHP',

    /* ---------- محدودیت‌ها ---------- */
    // حداکثر حجم هر فایل (بایت) → ۲ گیگابایت
    'max_file_size'      => 2 * 1024 * 1024 * 1024,
    // حجم هر تکه (chunk) در آپلود تکه‌تکه؛ خودکار با محدودیت‌های سرور تنظیم می‌شود
    'chunk_size'         => 8 * 1024 * 1024,
    // حداقل/حداکثر تعداد فایل در هر بار
    'max_files_per_batch' => 50,
    // سقف کل فضای اشغال‌شده (بایت). 0 = بدون محدودیت
    'max_storage_bytes'  => 0,
    // سقف تعداد آپلود در ساعت برای هر IP (0 = بدون محدودیت)
    'max_uploads_per_ip_per_hour' => 200,
    // عمر تکه‌های نیمه‌کاره (ثانیه) – بعد از این مدت پاک می‌شوند
    'session_ttl'        => 24 * 3600,
    // اجازه‌ی حذف فایل با کلید مخفی که موقع آپلود ساخته می‌شود
    'allow_delete'       => true,

    /* ---------- نوع فایل‌ها ---------- */
    // پسوندهای خطرناک: به‌جای پسوند اصلی با .bin ذخیره می‌شوند تا روی سرور اجرا نشوند
    'blocked_extensions' => [
        'php', 'php2', 'php3', 'php4', 'php5', 'php6', 'php7', 'php8', 'phps', 'phtml', 'pht',
        'phar', 'shtml', 'htaccess', 'htpasswd', 'cgi', 'fcgi', 'pl', 'py', 'rb', 'sh', 'bash',
        'asp', 'aspx', 'ashx', 'jsp', 'jspx', 'cfm', 'shtm', 'htc', 'ini', 'userini',
    ],
    // پسوندهای ممنوع (کلا آپلود نشوند). خالی بگذار تا همه چیز مجاز باشد.
    'denied_extensions'  => [],
    // پسوندهایی که فایل‌شان با نام اصلی ذخیره می‌شود (در غیر این صورت .bin)
    // نکته: فایل‌ها همیشه با نام تصادفی ذخیره می‌شوند، پسوند فقط برای تشخیص نوع است.
    // انواعی که مستقیم داخل مرورگر نمایش داده می‌شوند (بقیه اجبارا دانلود می‌شوند)
    'inline_mimes'       => [
        'image/jpeg', 'image/png', 'image/gif', 'image/webp', 'image/bmp', 'image/avif',
        'video/mp4', 'video/webm', 'video/ogg', 'audio/mpeg', 'audio/ogg', 'audio/wav',
        'audio/webm', 'application/pdf',
    ],

    /* ---------- لینک‌ها ---------- */
    // آدرس پایه برای ساختن لینک‌ها. خالی = تشخیص خودکار از روی درخواست
    'base_url'      => '',
    // لینک‌های کوتاه مثل /d/ab12cd34 (نیازمند فعال بودن mod_rewrite یا router.php)
    'pretty_urls'   => true,

    /* ---------- ذخیره‌سازی ---------- */
    // مسیر پوشه‌ی ذخیره‌سازی. خالی = پوشه storage کنار همین پروژه (یا مقدار متغیر محیطی APP_STORAGE_DIR)
    'storage_dir'   => '',
    // چک‌سام md5 برای فایل‌های کوچک‌تر از این حجم محاسبه می‌شود (0 = هرگز)
    'hash_max_bytes' => 64 * 1024 * 1024,

    /* ---------- ظاهر ---------- */
    'default_theme' => 'dark',      // dark | light
    'accent'        => '#6c5ce7',   // رنگ اصلی پنل
    'accent2'       => '#00d2ff',   // رنگ دوم گرادیان
    'accent3'       => '#ff5e9c',   // رنگ سوم (هاله‌ها)
    'show_recent'   => true,        // نمایش لیست فایل‌های اخیر در پنل
    'recent_limit'  => 12,

    /* ---------- امنیت / نگهداری ---------- */
    // ثبت رخدادها در storage/data/app.log
    'enable_log'    => true,
    // پاکسازی خودکار تکه‌های قدیمی به‌صورت تصادفی (احتمال 1 از N درخواست)
    'auto_cleanup_probability' => 40,
];
